"""bank.mcp — a deterministic personal-finance analysis suite.

Financial math is plain, unit-tested Python. Optional model paths narrate summaries,
match merchant names, extract receipt candidates, propose merchant contacts, and
draft prose. Raw bank transaction rows never enter a model prompt.
"""

__version__ = "0.1.0"
